import styled from 'styled-components'
import theme from 'theme_'

export const StyledDropdownError = styled.p`
  margin: 0;
  font-size: 12px;
  line-height: 16px;
  min-height: 16px;
  margin-bottom: ${theme.spacing.s};
  color: ${theme.colors.error};
  margin-top: 0;
`
